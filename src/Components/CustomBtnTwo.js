import React from 'react';

const CustomBtnTwo = ({children}) => {
    return (
        <button className='border border-gray-300 px-10 py-2 uppercase font-semibold rounded-full '>{children}</button>
    );
};

export default CustomBtnTwo;